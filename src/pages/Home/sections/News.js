import { Box } from "@mui/material";

export const News = () => {
  return (
    <Box
      sx={{
        backgroundColor: "background.paper",
        py: 15,
      }}
    >
      ff
    </Box>
  );
};
